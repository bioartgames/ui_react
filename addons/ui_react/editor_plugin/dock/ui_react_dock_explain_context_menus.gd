## Shared caps and copy strings for graph selection / details context ([UiReactDockExplainPanel]).
class_name UiReactDockExplainContextMenus
extends Object

const INCIDENT_EDGE_CAP := 8
const OTHER_EDGES_AT_ANCHOR_CAP := 6
const CYCLE_SUMMARY_CAP := 2
## Indented line under a registry binding row: matching BINDING edge in this graph.
const CONNECTIONS_BINDING_EDGE_CONTINUATION := "  └ "
const DETAILS_DECLARATIVE_ONE_LINER_BB := (
	"[i]Declarative snapshot only (not a runtime trace); cycle summaries are static candidates.[/i]\n"
)
